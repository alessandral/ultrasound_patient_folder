require 'spec_helper'

describe UltrasoundDriver::ProcessDirectory do
  context "initialize" do
    it "accepts a initial path and a destination path "
    it "should initialize an array with a list of patients "


    context "work directory" do
      it "calls read main folder"

      it "calls process folders"

    end

    context "#reads main folder" do

    end

    context "creates directory" do
      it "creates a directory when it doesn't exist"

    end

  end
end
